# Orchestration Log: hicks-1 (Backend Tests — Initial)

**Timestamp:** 2026-06-01T20:43:39Z  
**Agent:** Hicks (Tester / QA)  
**Phase:** v1 Backend Test Suite (Initial, Pre-Implementation)

## Assignments

Write full v1 backend test suite against documented API contract. Tests intentionally RED until Parker lands service/route modules.

## Deliverables

### Files Created
- `src/tests/conftest.py` — Shared fixtures: `mock_project_client`, `agent_version_factory`, `async_iter`, `_AsyncIter` class
- `src/tests/api/services/test_agent_definition.py` — 10 tests (9 parity invariants + 1 SDK-origin guard)
- `src/tests/api/services/test_agent_admin_service.py` — 6 service unit tests
- `src/tests/api/api/test_admin_routes.py` — 13 route unit tests
- `src/tests/api/api/test_admin_routes_functional.py` — 1 functional integration smoke test

## Test Coverage

**30 backend tests total** (initial state: intentionally RED pending Parker impl)

### Fixture Design
- **mock_project_client** — `MagicMock(spec=AIProjectClient)` with fully-typed async operations
- **agent_version_factory** — Auto-incrementing version IDs (`ver-0001`, `ver-0002`), fixed ISO 8601 timestamp to prevent JSON leakage
- **async_iter** — Minimal async-for stub for paginator simulation
- **_AsyncIter** — Protocol-matching async iterator class

### Parity Invariants (9 locked + 1 SDK guard = 10 tests)
1. FunctionTool name: `"get_sql_response"`
2. FunctionTool description: `"Execute T-SQL queries on the database..."`
3. FunctionTool param schema type: `"object"`
4. FunctionTool param schema required: `["sql_query"]`
5. FunctionTool param `sql_query` type: `"string"`
6. AzureAISearch tool present (exactly one)
7. AzureAISearch `query_type`: `"vector_simple"`
8. AzureAISearch `top_k`: `5`
9. Agent name template: `"KM-ConversationAgent-{solutionName}"`
10. Right SDK client: Imports from `azure.ai.projects.aio.operations`, not `azure.ai.agents.aio`

## Status

✅ **Complete** — 30 tests written, await Parker implementation to turn GREEN

## Blockers Defined

Listed assumptions about Parker's surface to unlock any misalignments early:

1. `AgentAdminService(project_client, agent_name, chat_service=None)` signature
2. Method signatures: `get_current() -> dict`, `publish(instructions, expected_version_id, publisher) -> dict`
3. `VersionConflictError` class exported from service
4. Route module structure: `api.admin_routes.router` with prefix `/api/admin`
5. Service patch site: `api.admin_routes.AgentAdminService`
6. Module-level `_idempotency_cache: dict` in `api/admin_routes.py`
7. `track_event_if_configured` importable from `api.admin_routes`
8. Functional test credential path: patches `services.agent_admin_service.AIProjectClient`

## Test Cases Not Yet Written

- `test_rollback_to_nonexistent_version_returns_404` — blocked on v2 (Parker's `GET/PUT /api/admin/agent/versions/{id}`)
- `test_models_list_sdk_attribute_error_fallback` — blocked on Parker's SDK error handling design
- Frontend RTL tests — blocked on Lambert's AgentAdmin.tsx implementation

## Notes for Downstream

All 17 pre-implementation tests are written against the exact API contract in the plan. Parker should align code to tests, or provide interface documentation for realignment.
